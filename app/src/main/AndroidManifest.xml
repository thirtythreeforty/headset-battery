<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.thirtythreeforty.headsetbattery">

    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver
            android:name=".HeadsetReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="@string/connection_changed_intent"/>
                <action android:name="@string/vendor_event_intent"/>
                <category android:name="@string/apple_vendor_category"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>